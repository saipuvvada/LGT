# Backend Directory

This project currently uses Supabase as a Backend-as-a-Service (BaaS), connecting directly from the frontend React application.

This directory is reserved for future backend logic such as:
- Supabase Edge Functions
- Custom Node.js/Express APIs
- Webhook handlers
- Scheduled cron jobs (via Supabase pg_cron or external triggers)
